<div id="errors">
<% unless @shopping_list.errors.messages.empty? %>
    <% @shopping_list.errors.messages.each do |field, error| %>
      <%= "#{field.capitalize}: #{error.first}" %><br />
    <% end %>
<% end %>
</div>

<div id="join">

  <h1> Make a New Shopping List </h1>
  <%= render "form" %>

</div>